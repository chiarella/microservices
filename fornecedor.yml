spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: '071283'
    url: jdbc:mysql://localhost/fornecedor?useTimezone=true&serverTimezone=UTC&DB_CLOSE_ON_EXIT=FALSE
    username: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true